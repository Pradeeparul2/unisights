[project]
name = "unisights"
version = "0.0.1-beta.15"
authors = [
  { name="Pradeep Arul", email="pradeeparul2@gmail.com" },
]
description = "Unisights Python SDK provides a lightweight event collection endpoint for Python web frameworks."
readme = "README.md"
requires-python = ">=3.12"

dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

license = "MIT"
license-files = ["LICEN[CS]E*"]

[project.urls]
Homepage = "https://github.com/Pradeeparul2/unisights"
Issues = "https://github.com/Pradeeparul2/unisights/issues"

[project.optional-dependencies]
test = [
    "pytest>=7.0",
    "pytest-asyncio>=0.20",
    "pytest-cov>=4.0",
]